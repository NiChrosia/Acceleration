const corrofluid = extend(Liquid, "corrofluid", {
	color: Color.valueOf("8FFE09"),
	explosiveness: 0.15,
	effect: StatusEffects.corroded,
	heatCapacity: 0.5,
	flammability: 0.6,
	temperature: 0.15,
	viscosity: 0.4
});